#include "../twenty.h"

void    add_here(t_froz *fz, char *s)
{
    int     i;
    // int     o;

    i = -1;
    while (s[++i])
    {
        if (s[i] == '<')
            ;
            if (fz)
                ;
    }
}